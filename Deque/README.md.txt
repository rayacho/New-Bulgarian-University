# Task Description

Implement the abstract data type (ADT) called Deque, or double-ended queue. A deque is a queue in which elements can be added and removed to/from both ends of the queue. Store integers inside the container.